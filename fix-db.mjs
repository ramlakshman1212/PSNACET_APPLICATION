import pg from 'pg'; const client = new pg.Client({ connectionString: 'postgres://webadmin:MAPlqk90284@node276505:5432/psnacet_admissions' }); async function main() { await client.connect(); await client.query('CREATE TABLE IF NOT EXISTS admin_settings (id BIGSERIAL PRIMARY KEY, key VARCHAR(255) UNIQUE NOT NULL, value TEXT, updated_at TIMESTAMPTZ DEFAULT NOW());'); await client.query('CREATE TABLE IF NOT EXISTS hostel_images (id SERIAL PRIMARY KEY, hostel_type TEXT NOT NULL, file_key TEXT NOT NULL, file_name TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());'); await client.query('CREATE TABLE IF NOT EXISTS student_documents (id SERIAL PRIMARY KEY, student_id UUID NOT NULL REFERENCES students (id) ON DELETE CASCADE, document_category TEXT NOT NULL, file_name TEXT NOT NULL, file_key TEXT NOT NULL, file_size BIGINT NOT NULL, file_type TEXT NOT NULL, uploaded_at TIMESTAMPTZ NOT NULL DEFAULT NOW());'); console.log('Tables created'); await client.end(); } main().catch(console.error);
