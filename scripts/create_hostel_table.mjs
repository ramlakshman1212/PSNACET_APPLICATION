import pg from 'pg'; const client = new pg.Client({ connectionString: 'postgresql://root:ram%401212@localhost:5432/management_app' }); client.connect().then(() => client.query('CREATE TABLE IF NOT EXISTS hostel_images ( id SERIAL PRIMARY KEY, hostel_type TEXT NOT NULL, file_key TEXT NOT NULL, file_name TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW() );')).then(() => { console.log('Table created'); process.exit(0); }).catch(console.error);
